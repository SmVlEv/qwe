@model UnityAssetStore.Models.Category
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Удалить категорию";
}

<div class="admin-container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="bi bi-trash me-2"></i> @ViewData["Title"]</h1>
        <a asp-area="Admin" asp-controller="Category" asp-action="Index"
           class="btn btn-admin btn-admin-outline-secondary">
            <i class="bi bi-arrow-left"></i> Назад
        </a>
    </div>

    <div class="alert alert-danger">
        <h4><i class="bi bi-exclamation-triangle"></i> Внимание!</h4>
        <p>Вы уверены, что хотите удалить категорию <strong>"@Model.Name"</strong>?</p>
        @if (Model.Assets?.Count > 0)
        {
            <p class="mt-2">
                <i class="bi bi-exclamation-circle"></i>
                Эта категория содержит @Model.Assets.Count товаров! Они будут перемещены в категорию "Без категории".
            </p>
        }
    </div>

    <div class="card admin-card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-4">ID категории:</dt>
                <dd class="col-sm-8">@Model.Id</dd>

                <dt class="col-sm-4">Название:</dt>
                <dd class="col-sm-8">@Model.Name</dd>

                

                <dt class="col-sm-4">Товаров в категории:</dt>
                <dd class="col-sm-8">@(Model.Assets?.Count ?? 0)</dd>
            </dl>

            <form asp-area="Admin" asp-controller="Category" asp-action="DeleteConfirmed" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-end mt-4">
                    <button type="submit" class="btn btn-admin btn-admin-danger">
                        <i class="bi bi-trash"></i> Подтвердить удаление
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>